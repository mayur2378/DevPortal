{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && turbo run build --filter=@devportal/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/mock/:path*", "destination": "http://localhost:3001/mock/:path*" }
  ]
}
